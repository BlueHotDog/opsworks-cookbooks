source 'https://supermarket.getchef.com'

cookbook 'nodejs', '~> 2.4.0'
cookbook 'nginx', '~> 2.7.6'
#cookbook 'apt', '~> 2.7.0'
#
#cookbook 'pm2', '~> 0.4.0'
# recipes = %w(haproxy                   opsworks_agent_monit      opsworks_ganglia          packages                  scm_helper
# apache2                   logs                      opsworks_aws_flow_ruby    opsworks_initial_setup    passenger_apache2         ssh_host_keys
# dependencies              memcached                 opsworks_berkshelf        opsworks_java             php                       ssh_users
# deploy                    mod_php5_apache2          opsworks_bundler          opsworks_nodejs           pm2
# ebs                       mysql                     opsworks_cleanup          opsworks_rubygems         rails
# nginx                     opsworks_commons          opsworks_shutdown         unicorn
# gem_support               nodejs-wrapper            opsworks_custom_cookbooks opsworks_stack_state_sync ruby
# pre_init_aws)
#
# recipes.each do |dep|
#  cookbook dep, :path => "./#{dep}"
# end